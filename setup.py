from setuptools import setup, find_packages
from clubready_booker import __version__

setup(
    name="clubready_booker",
    version=__version__,
    packages=find_packages(),
    author="Ryan A. Mannion"
)
